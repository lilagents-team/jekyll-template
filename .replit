modules = ["web", "nodejs-20", "bash", "ruby-3.2"]
run = "npm run start"

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "bundle exec jekyll serve --host=0.0.0.0 --port=5000"]
build = ["sh", "-c", "bundle install && bundle exec jekyll build"]

[workflows]
runButton = "Dev Server"

[[workflows.workflow]]
name = "Dev Server"
author = 41859832
mode = "parallel"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "npm run bookshop-dev"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "cd site && bundle exec jekyll serve --host=0.0.0.0 --port 6060 --trace --unpublished"

[[ports]]
localPort = 6060
externalPort = 3000

[[ports]]
localPort = 30775
externalPort = 80
